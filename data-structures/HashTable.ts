/** What is a hash table?
 * "Object" and "Map" in JavaScript
 * Used to store key-value pairs
 * Like array, but keys are not ordered
 * Unlike array, hash tables are fast in:
 * - finding values
 * - adding new values
 * - removing values
 */
